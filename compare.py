def test(string1,string2):
    'Tests to see if which string comes first alphabeticaly'
    'Parameters:The two strings'
    'Returns:Nothing, it prints which is first'
    for i in range(len(string1)):
        if string1[i]
